\usepackage {color} % Required for custom colors
\usepackage {fancyhdr} % Customize the header and footer
\usepackage [T1] {fontenc} % Output font encoding for international characters
\usepackage {fontspec} % Required for specification of custom fonts
\usepackage [ % Use A4 paper and set margins
    a4paper,
    hmargin = 20mm,
    vmargin = 15mm,
    top = 12mm
] {geometry}
\usepackage {hyperref} % Colors for links, text and headings
\usepackage {ifxetex} % Support for \ifxetex
\usepackage {ifthen} % Easy support for if-then-else statements
\usepackage {import} % Provides easy \subimport command
\usepackage {multicol} % Support for \multicols
\usepackage {pgfkeys} % Support named options for long macros
\usepackage {sectsty} % Allows customization of titles

\ifxetex \else
    \usepackage [utf8] {inputenc} % Required for inputting international characters. XeTeX is utf8 by default and fails if imported.
\fi
